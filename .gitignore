# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
/dist/
/build/
.venv/
.uv/

# Environment
.env

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Knowledge layer (private thinking — never commit)
knowledge/

# Tool caches (keep .claude/settings.json committed)
.claude/settings.local.json
.obsidian/

# ----- leak-prevention patterns (auto-added 2026-04-24) -----
# Block accidental commit of strategy / billing / outreach / pricing files.
# These categories should live in the gitignored hub working directory, never
# in a public repo. Use `git add -f` only after deliberate review.
*_prep.md
*_billing*
*_invoice*
*_outreach*
*_strategy*
*_positioning*
*_pricing*
*_buyer*
*_pitch*
product_direction.md
PORTFOLIO_REFRAME*.md
PLAN-OUTREACH*.md
